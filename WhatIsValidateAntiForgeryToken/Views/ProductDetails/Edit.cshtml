@model WhatIsValidateAntiForgeryToken.Models.ProductDetails

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    
      @*  @Html.AntiForgeryToken()
      *@

    <fieldset>
        <legend>ProductDetails</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.AccountUserName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AccountUserName)
            @Html.ValidationMessageFor(model => model.AccountUserName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ProductName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ProductName)
            @Html.ValidationMessageFor(model => model.ProductName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.LicenseCount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LicenseCount)
            @Html.ValidationMessageFor(model => model.LicenseCount)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.LicenseUsedCount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LicenseUsedCount)
            @Html.ValidationMessageFor(model => model.LicenseUsedCount)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
